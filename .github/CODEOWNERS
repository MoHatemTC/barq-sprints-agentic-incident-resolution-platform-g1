# Review routing by workstream. Owners are advisory here — the repository has no
# branch protection, so these are a signal about who knows the area, not a gate.

# S1.1 — scoped application and Incident field model. The field model is the
# contract every other workstream writes against, so schema changes need its owner.
/servicenow/                @ali-ezz
/docs/sprint1_field_model.md @ali-ezz

# S1.2 — audit trail, OAuth identity, ACLs
/scripts/verify_permissions.py  @MohamedAbdelaiem

# S1.5 — ServiceNow Table API client
/src/app/clients/   @Tasneemmohammed0
/src/app/auth/      @Tasneemmohammed0

# Build, CI and repository configuration
/.github/           @ali-ezz
